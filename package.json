{
  "name": "git-world-action",
  "version": "1.0.0",
  "description": "Generate an animated SVG city from your GitHub contributions",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build src/index.ts -o dist --minify",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@octokit/graphql": "^8.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.5.4"
  }
}
